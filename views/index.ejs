<!DOCTYPE html>
<html>
<head>
    <title>OVHCloud Node App</title>
</head>
<body>
    <h1>Item List</h1>
    <ul>
        <% items.forEach(item => { %>
            <li><%= item.name %></li>
        <% }) %>
    </ul>
    <form action="/add" method="POST">
        <input type="text" name="name" placeholder="New Item" required />
        <button type="submit">Add Item</button>
    </form>
</body>
</html>
